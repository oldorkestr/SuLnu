@model IEnumerable<SuLnu.Models.NewsViewModel>

@{
    ViewData["Title"] = "AllNews";
}

<style>
    th,td{
        color:black
    }
</style>
<h1>AllNews</h1>

<p>
    <a asp-action="CreateNews">Add News</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Tilte)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Description)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.PhotoFilePath)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Tilte)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Description)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.PhotoFilePath)
            </td>
            <td>
                @* ReSharper disable once Mvc.ActionNotResolved *@
                @Html.ActionLink("Edit", "Edit", new { /* id=item.PrimaryKey */ }) |
                @* ReSharper disable once Mvc.ActionNotResolved *@
                @Html.ActionLink("Details", "Details", new { /* id=item.PrimaryKey */ }) |
                @* ReSharper disable once Mvc.ActionNotResolved *@
                @Html.ActionLink("Delete", "Delete", new { /* id=item.PrimaryKey */ })
            </td>
        </tr>
}
    </tbody>
</table>
